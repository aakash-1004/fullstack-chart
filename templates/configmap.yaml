apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: {{ .Values.namespace }}
data:
  MONGO_DB: {{ .Values.config.mongoDb | quote }}
  MONGO_COLLECTION: {{ .Values.config.mongoCollection | quote }}
  FLASK_BACKEND: {{ .Values.config.flaskBackend | quote }}
